import { sum } from "./math";
import { foo, bar } from "./utils/index";
import loadsh from "loadsh";
// import jquery from "jquery";

const message = "dicaprio";

console.log(message);
console.log(sum(2, 3));

foo.format();
bar.format();

console.log(loadsh.join(["abc", "cba"]));
console.log(leo);
console.log(leoDicaprio());

$("body").html("<div>Dicaprio</div>");

let foz: foo = {
  foo: "foo",
};
console.log(foz);
