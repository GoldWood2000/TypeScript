declare module "loadsh" {
  export function join(arr: ang): void;
}

//声明全局变量 函数
declare const leo: number;
declare function leoDicaprio(arr?: number[]): number;

//声明全局接口
interface foo {
  foo: string;
}
